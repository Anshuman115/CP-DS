#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    while (t--)
    {
        /* code */
        int n;
        cin >> n;
        int arr[n];
        for (int i = 0; i < n; i++)
        {
            /* code */
            cin >> arr[i];
        }
    }

    return 0;
}