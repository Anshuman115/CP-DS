#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        /* code */
        int a, b;
        cin >> a >> b;
        int c = a % b;
        cout << c << endl;
    }

    return 0;
}